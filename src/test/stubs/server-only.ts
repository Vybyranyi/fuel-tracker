/**
 * Заглушка пакета `server-only` для тестів.
 *
 * Справжній пакет кидає виняток усюди, крім контексту React Server Components,
 * тож у Vitest будь-який серверний модуль падав би вже на імпорті. Захист від
 * потрапляння в клієнтський бандл забезпечує складання Next, а не тести, тому
 * підмінити його тут безпечно.
 */
export {};
